# 导波PDI成像应用 - 幅值加权版

## 版本信息
- 版本：v2.0
- 功能：多时间窗幅值加权、双方向特征提取与PDI成像
- 关键修复：熵值融合时对双方向加权结果进行了转置，避免列向量拼接导致的维度问题

## 主要功能
1. 读取两个方向的CSV数据
2. 提取幅值和小波包熵特征
3. 扫描多个时间窗，进行幅值加权
4. 分别显示方向1、方向2的加权熵值
5. 基于两方向数据进行PDI成像

## 使用流程
1. 选择路径1和路径2
2. 点击“提取特征”
3. 点击“快速对比”
4. 点击“幅值加权”
5. 点击“开始成像”

## 关键参数
- 时间窗：特征截断时间
- 平滑σ：曲线平滑系数
- 步距：射线间隔
- 椭圆Δ：PDI成像参数
- 权重σ：空间权重参数
- 分辨率：重建网格大小

## 输出图像
- 方向1幅值
- 方向1熵值
- 方向1加权熵值
- 方向2幅值
- 方向2熵值
- 方向2加权熵值
- 幅值PDI
- 加权熵值PDI（两方向融合）

## 注意事项
- 加权熵值必须与 PDI 输入维度一致
- 若出现只显示一个方向的问题，请检查：
  ```matlab
  entropy = [app.entropy_weighted_1' app.entropy_weighted_2'];
  ```
- 这里的转置是必须的，因为 `entropy_weighted_1` 和 `entropy_weighted_2` 是列向量

## 文件说明
- `GuidedWavePDIApp.m`：主程序
- `README.md`：说明文档

## 依赖环境
- MATLAB
- Signal Processing Toolbox
- Wavelet Toolbox
- Image Processing Toolbox

## 版本修复记录
- 修复了双方向熵值融合维度错误
- 增加了多时间窗幅值加权
- 增加了方向2加权结果显示
- 优化了成像流程
